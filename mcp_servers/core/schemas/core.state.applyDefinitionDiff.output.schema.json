{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "core.state.applyDefinitionDiff Output Schema",
  "description": "Output data for the core.state.applyDefinitionDiff MCP tool.",
  "type": "object",
  "properties": {
    "resulting_revision": {
      "type": "string",
      "description": "The resulting Git revision (commit hash) after applying the diff."
    }
  },
  "required": [
    "resulting_revision"
  ],
  "additionalProperties": false
}
